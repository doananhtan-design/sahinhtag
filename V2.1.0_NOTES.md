# SA HÌNH AI V2.1.0

Nền: V1.4.15-STABLE.

## Ổn định audio
- B08 TAG 82 và B12 TAG 123 dùng luồng audio ưu tiên riêng, không bị audio của bài trước hủy bởi `audioPlaySeq`.
- Nếu `audio/b08/dungxe.mp3` hoặc `audio/b12/dungxe.mp3` không phát được, fallback `audio/b03/dungxe.mp3`.

## B04/B06
- TAG 41/61 bắt đầu 120s.
- Hết 120s mà chưa có TAG 42/62: phát `quatg1.mp3` một lần để cảnh báo, không kết thúc bài.
- Đồng hồ 120s được cập nhật độc lập với detector.

## KT/B14
- TAG 141 khởi động bộ đếm 3s độc lập với camera.
- Sau 3s phát `hoanthanh.mp3` hai lần; đủ hai lần thì tự reset trạng thái và chờ TAG 01, giữ camera.

## Nhận TAG
- Vẫn dùng detector 36h11 và chọn TAG gần nhất theo diện tích.
- Thêm giữ TAG gần nhất khoảng 220ms để giảm nhảy TAG giữa các frame.

## B11
- Giữ khoảng cách cục bộ theo xe và công thức tốc độ của V1.4.15. Không tự thay audio `tunv.mp3` vì file này không có trong bộ audio chuẩn.
