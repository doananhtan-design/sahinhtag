# V1.3.6 — Rule vị trí + âm thanh + timer B08/B12

B02 / B03 / B10:
- TAG vào bài (21 / 31 / 101) -> `vaobai.mp3`.
- TAG kiểm tra (22 / 32 / 102) -> `dungxe.mp3` và kiểm tra vị trí.

B08 / B12:
- TAG vào bài (81 / 121) -> `vaobai.mp3` và bắt đầu bộ đếm 120 giây.
- TAG kiểm tra (82 / 123) -> `dungxe.mp3` + kiểm tra vị trí.
- TAG vào bài xuất hiện lại lần 2 sau khi đã rời khung -> dừng bộ đếm 120 giây và hoàn tất bài.
- Hết 120 giây chưa thấy TAG vào bài lần 2 -> `quatgbai.mp3`.
- Nút ghi nhận vị trí chỉ hiển thị ở B02/B03/B08/B10/B12 sau TAG kiểm tra.
